import 'package:flutter/material.dart';

class ColorsConfig {
  static const Color primary = Color(0xFF3a4356);
  static const Color primaryLightened = Color(0xFF404a5e);
  // static const Color accent = Colors.green;
  // static const Color accent = Color(0xFF5dcf84);
  // static const Color accent = Color(0xFF55d4b8);
  // static const Color accent = Color(0xFF439482);
  // static const Color accent = Color(0xFF2c989c);
  // static const Color accent = Color(0xFF2f944f);
  // static const Color accent = Color(0xFF4e9464);
  // static const Color accent = Color(0xFF4e9456);
  // static const Color accent = Color(0xFF4caf50);
  // static const Color accent = Color(0xFF4a9e55);
  static const Color accent = Color(0xFF4d9957);
  // static const Color accent = Color(0xFF);
  // static const Color accent = Color(0xFF);

  static const Color text = Colors.white;
}
